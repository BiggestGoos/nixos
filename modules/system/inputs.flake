szy:
{
	inputs =
	{

		nixpkgs.url = szy.lib.attrsets.mkDefault "github:NixOS/nixpkgs/nixos-unstable";

		home-manager = 
		{
			url = szy.lib.attrsets.mkDefault "github:nix-community/home-manager";
			inputs.nixpkgs.follows = "nixpkgs";
		};

	};
}
